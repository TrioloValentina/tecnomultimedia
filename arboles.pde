/*class arboles(){
  
//PROPIEDADES
  
 arboles(posX, posY, tam){
   posX =...
   posY =...
   tam = ...
 }
 PImage [] arbol = new PImage[2]; (por cada objeto arboles)
 int c = 0;
 
  for( int i = 0 ; i < arbol.length ; i++ ){
    arbol[i] = loadImage( "arbol"+i+".png" );
  }
//METODOS
  void drawA(){
    se dibujan los 4 objetos (4 arboles) con PImage en sus posiciones y tamaños
  }
  
  void KeyPressed(){
    if (keyCode == RIGHT){
     c++
     if ( c==arbol.length)
     c = 0;
    }
  }
*/
